@model PestControlDll.Entities.Route

@{
    ViewBag.Title = "DisplayDestinations";
}

<h2>DisplayDestinations</h2>

@if (Model != null)
{

    for (int i = 0; i < Model.Destinations.Count; i++)
    {
        <p>@Model.Destinations[i]</p>
    }
}